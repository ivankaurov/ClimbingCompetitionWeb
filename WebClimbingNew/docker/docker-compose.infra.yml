version: "3.4"
services:
  postgres-sql:
    image: postgres:latest
    environment:
      - POSTGRES_PASSWORD=P@ssw0rd
    networks:
      my-sql:
        aliases:
          - postgres-sql
  
networks:
  my-sql: